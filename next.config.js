/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        "covers.openlibrary.org",
        "images.unsplash.com",
      ],
    },
  };
  
  module.exports = nextConfig;
  